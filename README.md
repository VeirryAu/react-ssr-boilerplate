# MapApp
App for listing specific object into a map

# Dependencies
* `yarn`

# Development
*  `yarn dev`

# Production
*  `yarn start:server`

# Branch
`$username` -> (Pull Request) -> `develop` -> (Pull Request) -> `master`